# Factorial

We can use unordered multiplication to define sequences like factorials. If you care about theory, I think this is sufficient to heuristically show that anything you can do with sets can be done just as well (and programmed much nicer) with lists.

- Replace `clique 1` with `clique 2`; this will return zero graphs for certain inputs, why is this? It looks neat either way
