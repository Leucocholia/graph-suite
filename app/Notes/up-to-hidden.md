# Up to with hidden

Often you want to get all the graphs of a certain kind *up to* \(n\). We can do this by sectioning off one block to not render. This can be done with `hidden`.

- Why do we need ordered convolution for this?
- Calculate the \(n\)th triangular number \((1 + 2 + ... + n)\).
